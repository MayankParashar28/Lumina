<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/header') %>
        <title>Reset Password - Lumina</title>
        <link rel="stylesheet" href="/css/auth.css">
</head>

<body>
    <%- include('./partials/nav', { searchQuery: '' }) %>

        <div class="auth-split-wrapper">
            <!-- Left Side: Image -->
            <div class="auth-side-image-container">
                <img src="/images/auth-side.png" alt="Creative Space" class="auth-side-image">
                <div class="auth-image-overlay">
                    <h2>Strength is in the details.</h2>
                    <p>A secure password is the first step towards protecting your digital legacy.</p>
                </div>
            </div>

            <!-- Right Side: Form -->
            <div class="auth-form-container">
                <div class="auth-content-box">
                    <header class="mb-5">
                        <h1 class="auth-title">Reset Password</h1>
                        <p class="auth-subtitle">Enter your new password below.</p>
                    </header>

                    <form action="/user/reset-password/<%= token %>" method="POST">
                        <div class="form-group mb-3">
                            <label for="password">New Password</label>
                            <input type="password" class="form-control" id="password" name="password"
                                placeholder="Enter new password (min 6 chars)" required minlength="6">
                        </div>

                        <div class="form-group mb-4">
                            <label for="confirmPassword">Confirm Password</label>
                            <input type="password" class="form-control" id="confirmPassword" name="confirmPassword"
                                placeholder="Confirm new password" required minlength="6">
                        </div>

                        <button type="submit" class="submit-btn">
                            Update Password
                        </button>
                    </form>
                </div>
            </div>
        </div>

        <%- include('./partials/footer') %>
</body>

</html>