<% if (typeof announcement !=='undefined' && announcement) { %>
    <!-- 📢 Active Announcement Marquee -->
    <div class="marquee-container"
        style="margin-bottom: 0; background-color: <%= announcement.type === 'danger' ? '#ffebee' : announcement.type === 'warning' ? '#fff3cd' : announcement.type === 'success' ? '#e8f5e9' : 'var(--bg-color)' %>; border-bottom: 1px solid var(--border-color);">
        <div class="marquee-content">
            <% for(let i=0; i<10; i++) { %>
                <span
                    style="color: <%= announcement.type === 'danger' ? '#c62828' : announcement.type === 'warning' ? '#856404' : announcement.type === 'success' ? '#2e7d32' : '#666' %>;">
                    <%= announcement.message %> •
                </span>
                <% } %>
        </div>
    </div>
    <% } else { %>
        <!-- 🎨 Default Branding Marquee -->
        <div class="marquee-container" style="margin-bottom: 0;">
            <div class="marquee-content">
                <% for(let i=0; i<4; i++) { %>
                    <span>BUILDING THE FUTURE • MINIMAL DESIGN • TECH INSIGHTS • FRESH PERSPECTIVES • </span>
                    <% } %>
            </div>
        </div>
        <% } %>