<footer class="site-footer-minimal">
    <div class="footer-content-minimal">
        <div class="footer-left">
            <span class="footer-logo-text">Lumina</span>
            <span class="footer-copyright">&copy; <%= new Date().getFullYear() %></span>
        </div>

        <div class="footer-center">
            <p class="footer-tagline">
                Built by Love · Crafted for thinkers and builders
            </p>
            <div class="footer-links">
                <a href="/about">About</a>
                <a href="/privacy">Privacy</a>
                <a href="/terms">Terms</a>
            </div>
        </div>

        <div class="footer-right">
            <!-- <a href="#" aria-label="Twitter"><i class="fab fa-twitter"></i></a>
            <a href="#" aria-label="GitHub"><i class="fab fa-github"></i></a> -->
        </div>
    </div>
</footer>

<!-- Scroll to Top Button -->
<button id="scrollToTop" class="btn btn-sm rounded-circle">
    <i class="bi bi-arrow-up"></i>
</button>

<script>
    document.addEventListener("DOMContentLoaded", () => {
        const scrollToTopBtn = document.getElementById("scrollToTop");
        if (scrollToTopBtn) {
            window.addEventListener("scroll", () => {
                scrollToTopBtn.style.display = window.scrollY > 400 ? "block" : "none";
            });
            scrollToTopBtn.addEventListener("click", () => {
                // If Lenis is active, use it for the scroll-to-top
                if (window.lenis) {
                    window.lenis.scrollTo(0);
                } else {
                    window.scrollTo({ top: 0, behavior: "smooth" });
                }
            });
        }
    });
</script>

<!-- 🌊 Lenis Smooth Scroll (High Performance Config) -->
<script src="https://unpkg.com/@studio-freight/lenis@1.0.42/dist/lenis.min.js"></script>
<script>
    // Optimized for ZERO jerkiness
    const lenis = new Lenis({
        duration: 1.2,
        // Linear-ish easing for consistent feel, avoiding "catch-up" stutter
        easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t)),
        direction: 'vertical',
        gestureDirection: 'vertical',
        smooth: true,
        mouseMultiplier: 1,
        smoothTouch: false, // Critical: Disable on touch to prevent scroll fighting
        touchMultiplier: 2,
    });

    // High-performance loop
    function raf(time) {
        lenis.raf(time);
        requestAnimationFrame(raf);
    }
    requestAnimationFrame(raf);

    window.lenis = lenis;
</script>